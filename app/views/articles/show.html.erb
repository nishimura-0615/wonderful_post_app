<p id="notice"><%= notice %></p>

<div class="card">
  <div class="card-header">
    <%= @article.title %>
  </div>
  <div class="card-body">
    <%= @article.content %>
  </div>
</div>

<p>
  <strong>タイトル:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>本文:</strong>
  <%= @article.content %>
</p>
<td><% if current_user&.id == @article.user_id %></td>
<%= link_to '編集', edit_article_path(@article), class: "btn btn-warning" %>
<% end %>
<%= link_to '一覧', articles_path,  class: "btn btn-secondary" %>
